#include <iostream>

using namespace std;

int main()
{
  long long n, m;

  cin >> n >> m;

  long long a = 1,b=1;
  for (long long i=1; i<=m; i++) {
    b*=i;
    a*=n-i;
  }

  cout << a/b << '\n';
  
  return 0;
}
